import { Link, useParams } from 'react-router-dom'
import { projects, notes } from '../data/site'
export function ProjectDetail(){const {slug}=useParams();const p=projects.find(x=>x.slug===slug); if(!p)return <NotFound/>; return <main><article className="case wrap"><div className="eyebrow">CASE STUDY</div><span className="case-type">{p.caseType}</span><h1>{p.title.replace(' API','')}<br/><em>API.</em></h1><p className="case-lead">{p.lead}</p><div className="case-tags">{p.tags.map(t=><b key={t}>{t}</b>)}</div><div className="case-grid">{p.sections.map(s=><article key={s[0]}><small>{s[0]}</small><h2>{s[1]}</h2><p>{s[2]}</p></article>)}</div><a className="button primary" href={p.repo} target="_blank" rel="noreferrer">View GitHub repository ↗</a></article></main>}
export function NoteDetail(){const {slug}=useParams();const n=notes.find(x=>x.slug===slug); if(!n)return <NotFound/>; return <main><article className="note wrap"><div className="eyebrow">{n.label}</div><h1>{n.title}</h1><p className="note-lead">{n.lead}</p>{n.sections.map(s=><section key={s[0]}><h2>{s[0]}</h2><p>{s[1]}</p></section>)}</article></main>}
export function NotFound(){return <main><article className="note wrap"><div className="eyebrow">404 / NOT FOUND</div><h1>Page not found.</h1><p className="note-lead">The page you requested does not exist.</p><Link className="button primary" to="/">Back home →</Link></article></main>}
